{
  "name": "server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd .. && bun run --env-file=.env --watch server/index.ts",
    "start": "cd .. && bun run --env-file=.env server/index.ts"
  },
  "dependencies": {
    "express": "^4.21.2",
    "helmet": "^8.0.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/cors": "^2.8.17"
  }
}
